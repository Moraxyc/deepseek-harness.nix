diff --git a/packages/dsh-desktop-launcher/src/index.ts b/packages/dsh-desktop-launcher/src/index.ts
index b1de3fd..5e4d898 100644
--- a/packages/dsh-desktop-launcher/src/index.ts
+++ b/packages/dsh-desktop-launcher/src/index.ts
@@ -14 +14 @@ import type { Context } from '@deepseek-ai/cordis'
-import { installSettingsSection, settingsNamespace } from '@deepseek-ai/dsh-settings'
+import type {} from '@deepseek-ai/dsh-settings'
@@ -44 +44 @@ export const inject = ['webServer', 'systemPrompt']
-export const DESKTOP_LAUNCHER_SETTINGS_NAMESPACE = settingsNamespace('desktop-launcher')
+export const DESKTOP_LAUNCHER_SETTINGS_NAMESPACE = 'desktop-launcher'
@@ -156,3 +156,5 @@ function applyImpl(ctx: Context, config?: Config): void {
-  installSettingsSection(ctx, DESKTOP_LAUNCHER_SETTINGS_NAMESPACE, Config, config ?? {}, {
-    setSource: (source) => { current = source; sync() },
-    onChange: sync,
+  ctx.inject(['settings'], (settingsCtx) => {
+    settingsCtx.settings.installSection(ctx, DESKTOP_LAUNCHER_SETTINGS_NAMESPACE, Config, config ?? {}, {
+      setSource: (source) => { current = source; sync() },
+      onChange: sync,
+    })
@@ -162 +164 @@ function applyImpl(ctx: Context, config?: Config): void {
-  // no settings service, whose installSettingsSection never fires its hooks).
+  // no settings service, whose settings registration never fires its hooks).
@@ -164 +166 @@ function applyImpl(ctx: Context, config?: Config): void {
-}
\ No newline at end of file
+}
diff --git a/packages/dsh-doctor/src/index.ts b/packages/dsh-doctor/src/index.ts
index 61f5356..0580b17 100644
--- a/packages/dsh-doctor/src/index.ts
+++ b/packages/dsh-doctor/src/index.ts
@@ -5 +5 @@ import type {} from '@deepseek-ai/dsh-host-webserver'
-import { installSettingsSection, settingsNamespace } from '@deepseek-ai/dsh-settings'
+import type {} from '@deepseek-ai/dsh-settings'
@@ -23 +23 @@ export const Config: z<Config> = z.object({ enabled: z.boolean().default(true),
-export const DOCTOR_SETTINGS_NAMESPACE = settingsNamespace('doctor')
+export const DOCTOR_SETTINGS_NAMESPACE = 'doctor'
@@ -68 +68,3 @@ export const apply = mountOnce('@linxin666/dsh-doctor', (ctx: Context, config?:
-  installSettingsSection(ctx, DOCTOR_SETTINGS_NAMESPACE, Config, config ?? {}, { setSource: source => { current = source; sync() }, onChange: sync })
+  ctx.inject(['settings'], (settingsCtx) => {
+    settingsCtx.settings.installSection(ctx, DOCTOR_SETTINGS_NAMESPACE, Config, config ?? {}, { setSource: source => { current = source; sync() }, onChange: sync })
+  })
diff --git a/packages/dsh-git-graph/src/client/index.ts b/packages/dsh-git-graph/src/client/index.ts
index 44ec00c..6f8a91e 100644
--- a/packages/dsh-git-graph/src/client/index.ts
+++ b/packages/dsh-git-graph/src/client/index.ts
@@ -18,0 +19,2 @@ import type { SessionId } from '@deepseek-ai/dsh-api-remotes/client'
+import type { SessionListState } from '@deepseek-ai/dsh-api-session-controller/client'
+import type { SnapshotSelectorHook } from '@deepseek-ai/dsh-client-store'
@@ -40,0 +43,15 @@ declare module '@deepseek-ai/dsh-client-ui-slots' {
+  interface GlobalStandardProps {
+    /** Session list selector supplied by the alpha.2 session adapter. */
+    useSessions: SnapshotSelectorHook<SessionListState>
+  }
+
+  interface SessionMaybeStandardProps {
+    /** Current session identity, absent while no session is selected. */
+    sessionId: SessionId | undefined
+  }
+
+  interface SessionStandardProps {
+    /** Current session identity for a session-scoped dock seat. */
+    sessionId: SessionId
+  }
+
diff --git a/packages/dsh-git-graph/src/index.ts b/packages/dsh-git-graph/src/index.ts
index deeffe3..2103747 100644
--- a/packages/dsh-git-graph/src/index.ts
+++ b/packages/dsh-git-graph/src/index.ts
@@ -21 +21 @@ import type {} from '@deepseek-ai/dsh-workspace'
-import { installSettingsSection, settingsNamespace } from '@deepseek-ai/dsh-settings'
+import type {} from '@deepseek-ai/dsh-settings'
@@ -51 +51 @@ export const Config: z<Config> = z.object({
-export const GIT_GRAPH_SETTINGS = settingsNamespace('git-graph')
+export const GIT_GRAPH_SETTINGS = 'git-graph'
@@ -93 +93 @@ function applyImpl(ctx: Context, config?: Config): void {
-  // The live config source: installSettingsSection swaps it when the user
+  // The live config source: the settings registration swaps it when the user
@@ -120,3 +120,5 @@ function applyImpl(ctx: Context, config?: Config): void {
-  installSettingsSection(ctx, GIT_GRAPH_SETTINGS, Config, effectiveConfig(config), {
-    setSource: (source) => { current = () => effectiveConfig(source()) },
-    onChange: syncTool,
+  ctx.inject(['settings'], (settingsCtx) => {
+    settingsCtx.settings.installSection(ctx, GIT_GRAPH_SETTINGS, Config, effectiveConfig(config), {
+      setSource: (source) => { current = () => effectiveConfig(source()) },
+      onChange: syncTool,
+    })
@@ -133 +135 @@ function applyImpl(ctx: Context, config?: Config): void {
-}
\ No newline at end of file
+}
diff --git a/packages/dsh-liangshen/src/index.ts b/packages/dsh-liangshen/src/index.ts
index 23fdc1f..49af38f 100644
--- a/packages/dsh-liangshen/src/index.ts
+++ b/packages/dsh-liangshen/src/index.ts
@@ -23 +23 @@ import type { Context } from '@deepseek-ai/cordis'
-import { installSettingsSection, settingsNamespace } from '@deepseek-ai/dsh-settings'
+import type {} from '@deepseek-ai/dsh-settings'
@@ -34 +34 @@ export const name = 'liangshen'
-export const LIANGSHEN_SETTINGS_NAMESPACE = settingsNamespace('dsh-liangshen')
+export const LIANGSHEN_SETTINGS_NAMESPACE = 'dsh-liangshen'
@@ -85 +85 @@ function applyImpl(ctx: Context, config?: Config): void {
-  // (installSettingsSection swaps it when the namespace registers).
+  // (the settings registration swaps it when the namespace registers).
@@ -129,6 +129,8 @@ function applyImpl(ctx: Context, config?: Config): void {
-  installSettingsSection(ctx, LIANGSHEN_SETTINGS_NAMESPACE, Config, config ?? {}, {
-    setSource: (source) => {
-      current = source
-      refresh()
-    },
-    onChange: refresh,
+  ctx.inject(['settings'], (settingsCtx) => {
+    settingsCtx.settings.installSection(ctx, LIANGSHEN_SETTINGS_NAMESPACE, Config, config ?? {}, {
+      setSource: (source) => {
+        current = source
+        refresh()
+      },
+      onChange: refresh,
+    })
diff --git a/packages/dsh-market/src/index.ts b/packages/dsh-market/src/index.ts
index 82b5651..04adbed 100644
--- a/packages/dsh-market/src/index.ts
+++ b/packages/dsh-market/src/index.ts
@@ -12 +12 @@ import type {} from '@deepseek-ai/dsh-host-webserver'
-import { installSettingsSection, settingsNamespace } from '@deepseek-ai/dsh-settings'
+import type {} from '@deepseek-ai/dsh-settings'
@@ -24 +24 @@ export const inject = ['webServer']
-export const MARKET_SETTINGS_NAMESPACE = settingsNamespace('dsh-web-ui-market')
+export const MARKET_SETTINGS_NAMESPACE = 'dsh-web-ui-market'
@@ -40,3 +40,5 @@ function applyImpl(ctx: Context): void {
-  installSettingsSection(ctx, MARKET_SETTINGS_NAMESPACE, Config, {}, {
-    setSource: () => { /* application is browser-side; value is read from the scope */ },
-    onChange: () => { /* browser half re-reads on scope publish */ },
+  ctx.inject(['settings'], (settingsCtx) => {
+    settingsCtx.settings.installSection(ctx, MARKET_SETTINGS_NAMESPACE, Config, {}, {
+      setSource: () => { /* application is browser-side; value is read from the scope */ },
+      onChange: () => { /* browser half re-reads on scope publish */ },
+    })
diff --git a/packages/dsh-perf/src/index.ts b/packages/dsh-perf/src/index.ts
index a0f332c..e93d902 100644
--- a/packages/dsh-perf/src/index.ts
+++ b/packages/dsh-perf/src/index.ts
@@ -3 +3 @@ import type {} from '@deepseek-ai/dsh-host-webserver'
-import { installSettingsSection, settingsNamespace } from '@deepseek-ai/dsh-settings'
+import type {} from '@deepseek-ai/dsh-settings'
@@ -12 +12 @@ export const inject = ['webServer']
-export const PERF_SETTINGS_NAMESPACE = settingsNamespace('dsh-perf')
+export const PERF_SETTINGS_NAMESPACE = 'dsh-perf'
@@ -116,3 +116,5 @@ export const apply = mountOnce('@linxin666/dsh-perf', (ctx: Context, config?: Co
-  installSettingsSection(ctx, PERF_SETTINGS_NAMESPACE, Config, config ?? {}, {
-    setSource: (next) => { source = next; rearm() },
-    onChange: rearm,
+  ctx.inject(['settings'], (settingsCtx) => {
+    settingsCtx.settings.installSection(ctx, PERF_SETTINGS_NAMESPACE, Config, config ?? {}, {
+      setSource: (next) => { source = next; rearm() },
+      onChange: rearm,
+    })
@@ -140 +142 @@ export const apply = mountOnce('@linxin666/dsh-perf', (ctx: Context, config?: Co
-})
\ No newline at end of file
+})
diff --git a/packages/dsh-pet/src/index.ts b/packages/dsh-pet/src/index.ts
index 10e907e..7b8c3f9 100644
--- a/packages/dsh-pet/src/index.ts
+++ b/packages/dsh-pet/src/index.ts
@@ -15 +15 @@ import { Context } from '@deepseek-ai/cordis'
-import { installSettingsSection, settingsNamespace } from '@deepseek-ai/dsh-settings'
+import type {} from '@deepseek-ai/dsh-settings'
@@ -192,12 +192,14 @@ function applyImpl(ctx: Context, config: PetConfig = {}): void {
-  installSettingsSection(
-    ctx,
-    settingsNamespace(PET_SETTINGS_NAMESPACE),
-    makePetSettingsSchema(service.selectedPetId()),
-    base,
-    {
-      setSource: (source) => { current = source },
-      onChange: () => {
-        const section = current()
-        service.applySettingsSection(section)
-        service.setEnabled(section.enabled ?? true)
-        syncRoutes()
+  ctx.inject(['settings'], (settingsCtx) => {
+    settingsCtx.settings.installSection(
+      ctx,
+      PET_SETTINGS_NAMESPACE,
+      makePetSettingsSchema(service.selectedPetId()),
+      base,
+      {
+        setSource: (source) => { current = source },
+        onChange: () => {
+          const section = current()
+          service.applySettingsSection(section)
+          service.setEnabled(section.enabled ?? true)
+          syncRoutes()
+        },
@@ -205,2 +207,2 @@ function applyImpl(ctx: Context, config: PetConfig = {}): void {
-    },
-  )
+    )
+  })
diff --git a/packages/dsh-remote-web-ui/src/index.ts b/packages/dsh-remote-web-ui/src/index.ts
index 8614183..b29e0ab 100644
--- a/packages/dsh-remote-web-ui/src/index.ts
+++ b/packages/dsh-remote-web-ui/src/index.ts
@@ -16 +16 @@ import type { Context } from '@deepseek-ai/cordis'
-import { installSettingsSection, settingsNamespace } from '@deepseek-ai/dsh-settings'
+import type {} from '@deepseek-ai/dsh-settings'
@@ -88 +88 @@ export const inject = ['webServer', 'typertGateway', 'connection']
-export const REMOTE_WEB_UI_SETTINGS_NAMESPACE = settingsNamespace('remote-web-ui')
+export const REMOTE_WEB_UI_SETTINGS_NAMESPACE = 'remote-web-ui'
@@ -263 +263 @@ function applyImpl(ctx: Context, config?: Config): void {
-  // otherwise (installSettingsSection swaps it when the namespace registers).
+  // otherwise (the settings registration swaps it when the namespace registers).
@@ -716,6 +716,8 @@ function applyImpl(ctx: Context, config?: Config): void {
-  installSettingsSection(ctx, REMOTE_WEB_UI_SETTINGS_NAMESPACE, Config, config ?? {}, {
-    setSource: (source) => {
-      current = source
-      sync()
-    },
-    onChange: sync,
+  ctx.inject(['settings'], (settingsCtx) => {
+    settingsCtx.settings.installSection(ctx, REMOTE_WEB_UI_SETTINGS_NAMESPACE, Config, config ?? {}, {
+      setSource: (source) => {
+        current = source
+        sync()
+      },
+      onChange: sync,
+    })
diff --git a/packages/dsh-ssh/src/index.ts b/packages/dsh-ssh/src/index.ts
index 6e1df9a..72c749b 100644
--- a/packages/dsh-ssh/src/index.ts
+++ b/packages/dsh-ssh/src/index.ts
@@ -12 +12 @@ import type { Context } from '@deepseek-ai/cordis'
-import { installSettingsSection, settingsNamespace } from '@deepseek-ai/dsh-settings'
+import type {} from '@deepseek-ai/dsh-settings'
@@ -34 +34 @@ export const inject = ['webServer', 'tools', 'systemPrompt']
-export const SSH_SETTINGS_NAMESPACE = settingsNamespace('dsh-ssh')
+export const SSH_SETTINGS_NAMESPACE = 'dsh-ssh'
@@ -165,7 +165,9 @@ function applyImpl(ctx: Context, config?: Config): void {
-  installSettingsSection(ctx, SSH_SETTINGS_NAMESPACE, Config, config ?? {}, {
-    setSource: (source) => {
-      isSettingsBound = true
-      current = source
-      sync()
-    },
-    onChange: sync,
+  ctx.inject(['settings'], (settingsCtx) => {
+    settingsCtx.settings.installSection(ctx, SSH_SETTINGS_NAMESPACE, Config, config ?? {}, {
+      setSource: (source) => {
+        isSettingsBound = true
+        current = source
+        sync()
+      },
+      onChange: sync,
+    })
@@ -175 +177 @@ function applyImpl(ctx: Context, config?: Config): void {
-  // no settings service, whose installSettingsSection never fires its hooks).
+  // no settings service, whose settings registration never fires its hooks).
diff --git a/packages/dsh-task-board/src/index.ts b/packages/dsh-task-board/src/index.ts
index 2e892bc..d7c030a 100644
--- a/packages/dsh-task-board/src/index.ts
+++ b/packages/dsh-task-board/src/index.ts
@@ -12 +12 @@ import type {} from '@deepseek-ai/dsh-commands'
-import { installSettingsSection, settingsNamespace } from '@deepseek-ai/dsh-settings'
+import type {} from '@deepseek-ai/dsh-settings'
@@ -40 +40 @@ export const TASK_BOARD_GUIDANCE = '本机已安装 dsh-task-board 插件（DSH
-export const TASK_BOARD_SETTINGS_NAMESPACE = settingsNamespace('task-board')
+export const TASK_BOARD_SETTINGS_NAMESPACE = 'task-board'
@@ -131 +131 @@ function applyImpl(ctx: Context, config?: Config): void {
-  // (installSettingsSection swaps it when the namespace registers).
+  // (the settings registration swaps it when the namespace registers).
@@ -154,3 +154,5 @@ function applyImpl(ctx: Context, config?: Config): void {
-  installSettingsSection(ctx, TASK_BOARD_SETTINGS_NAMESPACE, Config, config ?? {}, {
-    setSource: (source) => { current = source },
-    onChange: sync,
+  ctx.inject(['settings'], (settingsCtx) => {
+    settingsCtx.settings.installSection(ctx, TASK_BOARD_SETTINGS_NAMESPACE, Config, config ?? {}, {
+      setSource: (source) => { current = source },
+      onChange: sync,
+    })
@@ -160 +162 @@ function applyImpl(ctx: Context, config?: Config): void {
-  // no settings service, whose installSettingsSection never fires its hooks).
+  // no settings service, whose settings registration never fires its hooks).
diff --git a/packages/dsh-tool-describe-image/src/config-resolve.ts b/packages/dsh-tool-describe-image/src/config-resolve.ts
index 91ff7f8..d77100b 100644
--- a/packages/dsh-tool-describe-image/src/config-resolve.ts
+++ b/packages/dsh-tool-describe-image/src/config-resolve.ts
@@ -15 +14,0 @@ import { launchEnvironmentOf } from '@deepseek-ai/dsh-launch-environment'
-import { settingsNamespace } from '@deepseek-ai/dsh-settings'
@@ -178 +177 @@ export const Config: z<Config> = z.object({
-export const DESCRIBE_IMAGE_SETTINGS_NAMESPACE = settingsNamespace('describe-image')
+export const DESCRIBE_IMAGE_SETTINGS_NAMESPACE = 'describe-image'
@@ -413 +411,0 @@ export async function resolveApiKey(
-
diff --git a/packages/dsh-tool-describe-image/src/index.ts b/packages/dsh-tool-describe-image/src/index.ts
index 424bfe7..5ba779e 100644
--- a/packages/dsh-tool-describe-image/src/index.ts
+++ b/packages/dsh-tool-describe-image/src/index.ts
@@ -18 +18 @@ import type { Context } from '@deepseek-ai/cordis'
-import { installSettingsSection } from '@deepseek-ai/dsh-settings'
+import type {} from '@deepseek-ai/dsh-settings'
@@ -150,14 +150,16 @@ function applyImpl(ctx: Context, config: Config = {}): void {
-  installSettingsSection(ctx, DESCRIBE_IMAGE_SETTINGS_NAMESPACE, Config, config, {
-    setSource: (source) => {
-      current = source
-    },
-    onChange: () => {},
-    validate: (value) => {
-      // The Host applies a batched edit op by op, so each intermediate state
-      // is judged too: a connection is only validated once both halves
-      // exist, otherwise baseURL alone (model not landed yet) or model alone
-      // would each refuse the other's op and strand the save. A partial
-      // config still fails loud at the first describe_image call.
-      if (value.baseURL !== undefined && value.model !== undefined) resolveConfig(value)
-      else if (Array.isArray(value.endpoints) && value.endpoints.length > 0) resolveConfig(value)
-    },
+  ctx.inject(['settings'], (settingsCtx) => {
+    settingsCtx.settings.installSection(ctx, DESCRIBE_IMAGE_SETTINGS_NAMESPACE, Config, config, {
+      setSource: (source) => {
+        current = source
+      },
+      onChange: () => {},
+      validate: (value) => {
+        // The Host applies a batched edit op by op, so each intermediate state
+        // is judged too: a connection is only validated once both halves
+        // exist, otherwise baseURL alone (model not landed yet) or model alone
+        // would each refuse the other's op and strand the save. A partial
+        // config still fails loud at the first describe_image call.
+        if (value.baseURL !== undefined && value.model !== undefined) resolveConfig(value)
+        else if (Array.isArray(value.endpoints) && value.endpoints.length > 0) resolveConfig(value)
+      },
+    })
@@ -292 +294 @@ function applyImpl(ctx: Context, config: Config = {}): void {
-}
\ No newline at end of file
+}
diff --git a/packages/dsh-tool-describe-image/src/native-images.ts b/packages/dsh-tool-describe-image/src/native-images.ts
index c15f051..5715b34 100644
--- a/packages/dsh-tool-describe-image/src/native-images.ts
+++ b/packages/dsh-tool-describe-image/src/native-images.ts
@@ -23 +23 @@ import type { Context } from '@deepseek-ai/cordis'
-import { settingsNamespace, type SettingsNamespace, type SettingsPathOp } from '@deepseek-ai/dsh-settings'
+import type { SettingsNamespace, SettingsPathOp } from '@deepseek-ai/dsh-settings'
@@ -29 +29 @@ import { optionalService, UNKNOWN_CAPABILITY, type InvalidatableRouteResolver, t
-export const LLM_DEEPSEEK_SETTINGS_NAMESPACE = settingsNamespace('llm-deepseek')
+export const LLM_DEEPSEEK_SETTINGS_NAMESPACE = 'llm-deepseek' as SettingsNamespace
@@ -71 +71 @@ function adapterNamespaceForRoute(route: { provider: string; model: string } | u
-    return settingsNamespace(`llm-${route.provider}`)
+    return `llm-${route.provider}` as SettingsNamespace
diff --git a/packages/dsh-usage/src/host/usage-service.ts b/packages/dsh-usage/src/host/usage-service.ts
index 6ffa5cc..25be961 100644
--- a/packages/dsh-usage/src/host/usage-service.ts
+++ b/packages/dsh-usage/src/host/usage-service.ts
@@ -16 +15,0 @@ import type { TokenUsage } from '@deepseek-ai/dsh-llm'
-import { settingsNamespace } from '@deepseek-ai/dsh-settings'
@@ -177 +176 @@ function readNamespace(ctx: Context, ns: string): unknown {
-    return settings.get(settingsNamespace(ns))
+    return settings.get(ns)
diff --git a/packages/dsh-usage/src/index.ts b/packages/dsh-usage/src/index.ts
index 0356cad..58839c2 100644
--- a/packages/dsh-usage/src/index.ts
+++ b/packages/dsh-usage/src/index.ts
@@ -3 +3 @@ import type {} from '@deepseek-ai/dsh-host-webserver'
-import { installSettingsSection, settingsNamespace } from '@deepseek-ai/dsh-settings'
+import type {} from '@deepseek-ai/dsh-settings'
@@ -11 +11 @@ export const inject = ['webServer']
-export const USAGE_SETTINGS_NAMESPACE = settingsNamespace('dsh-usage')
+export const USAGE_SETTINGS_NAMESPACE = 'dsh-usage'
@@ -88,3 +88,5 @@ export const apply = mountOnce('@linxin666/dsh-usage', (ctx: Context, config?: C
-  installSettingsSection(ctx, USAGE_SETTINGS_NAMESPACE, Config, config ?? {}, {
-    setSource: (next) => { source = next; rearm() },
-    onChange: rearm,
+  ctx.inject(['settings'], (settingsCtx) => {
+    settingsCtx.settings.installSection(ctx, USAGE_SETTINGS_NAMESPACE, Config, config ?? {}, {
+      setSource: (next) => { source = next; rearm() },
+      onChange: rearm,
+    })
diff --git a/packages/dsh-web-settings/src/bridge.ts b/packages/dsh-web-settings/src/bridge.ts
index c76299f..c3d0cbe 100644
--- a/packages/dsh-web-settings/src/bridge.ts
+++ b/packages/dsh-web-settings/src/bridge.ts
@@ -18 +18 @@ import type { SettingsNamespace, SettingsDescriptor, SettingsPathOp, SettingsPro
-import { SettingsConflictError, settingsNamespace } from '@deepseek-ai/dsh-settings'
+import { SettingsConflictError } from '@deepseek-ai/dsh-settings'
@@ -214 +214 @@ export function makeBridgeHandlers(deps: BridgeDeps): BridgeHandlers {
-        await deps.settings.mutate(settingsNamespace(ns) as SettingsNamespace, body.ops as SettingsPathOp[], expectedRevision)
+        await deps.settings.mutate(ns as SettingsNamespace, body.ops as SettingsPathOp[], expectedRevision)
diff --git a/packages/skins/skin-center/src/index.ts b/packages/skins/skin-center/src/index.ts
index 62e4f3d..514e673 100644
--- a/packages/skins/skin-center/src/index.ts
+++ b/packages/skins/skin-center/src/index.ts
@@ -12 +12 @@ import { Context } from '@deepseek-ai/cordis'
-import { installSettingsSection, settingsNamespace } from '@deepseek-ai/dsh-settings'
+import type {} from '@deepseek-ai/dsh-settings'
@@ -58 +58 @@ export const inject = ['webServer']
-export const SKIN_BACKGROUND_NAMESPACE = settingsNamespace('skin-background')
+export const SKIN_BACKGROUND_NAMESPACE = 'skin-background'
@@ -61 +61 @@ export const SKIN_BACKGROUND_NAMESPACE = settingsNamespace('skin-background')
-export const SKIN_CUSTOM_THEME_NAMESPACE = settingsNamespace(SKIN_CUSTOM_THEME_NS)
+export const SKIN_CUSTOM_THEME_NAMESPACE = SKIN_CUSTOM_THEME_NS
@@ -109 +109 @@ export const SkinBackgroundConfigSchema: z<SkinBackgroundConfig> = z.object({
-export const SKIN_WALLPAPER_NAMESPACE = settingsNamespace('skin-wallpaper')
+export const SKIN_WALLPAPER_NAMESPACE = 'skin-wallpaper'
@@ -164 +164 @@ function applyImpl(ctx: Context): void {
-  // re-resolves across reloads. installSettingsSection is a no-op when no
+  // re-resolves across reloads. The settings registration is a no-op when no
@@ -166,18 +166,20 @@ function applyImpl(ctx: Context): void {
-  installSettingsSection(ctx, SKIN_BACKGROUND_NAMESPACE, SkinBackgroundConfigSchema, {}, {
-    setSource: (source) => {
-      // Issue #996: the authoritative store is now the v2 active-state
-      // document (reachable through the remote pairing channel); this legacy
-      // namespace stays as the official settings page's input face. Copy a
-      // customized legacy section into the v2 store exactly once — safe at
-      // detach too, since the entry fallback resolves to schema defaults,
-      // which hasCustomSkinBackground excludes.
-      const migration = migrateBackgroundFromSettings({
-        activeStatePath: defaultActiveStatePath(),
-        readSettings: source,
-      })
-      for (const note of migration.notes) {
-        if (migration.migrated) console.info(`[ui-skin-center] background migration: ${note}`)
-        else console.error(`[ui-skin-center] background migration: ${note}`)
-      }
-    },
-    onChange: () => { /* browser half re-applies on scope publish and persists via the v2 channel */ },
+  ctx.inject(['settings'], (settingsCtx) => {
+    settingsCtx.settings.installSection(ctx, SKIN_BACKGROUND_NAMESPACE, SkinBackgroundConfigSchema, {}, {
+      setSource: (source) => {
+        // Issue #996: the authoritative store is now the v2 active-state
+        // document (reachable through the remote pairing channel); this legacy
+        // namespace stays as the official settings page's input face. Copy a
+        // customized legacy section into the v2 store exactly once — safe at
+        // detach too, since the entry fallback resolves to schema defaults,
+        // which hasCustomSkinBackground excludes.
+        const migration = migrateBackgroundFromSettings({
+          activeStatePath: defaultActiveStatePath(),
+          readSettings: source,
+        })
+        for (const note of migration.notes) {
+          if (migration.migrated) console.info(`[ui-skin-center] background migration: ${note}`)
+          else console.error(`[ui-skin-center] background migration: ${note}`)
+        }
+      },
+      onChange: () => { /* browser half re-applies on scope publish and persists via the v2 channel */ },
+    })
@@ -186,7 +188,9 @@ function applyImpl(ctx: Context): void {
-  installSettingsSection(ctx, SKIN_CUSTOM_THEME_NAMESPACE, SkinCustomThemeConfigSchema, {
-    ...CUSTOM_THEME_DEFAULTS,
-    light: { ...CUSTOM_THEME_DEFAULTS.light },
-    dark: { ...CUSTOM_THEME_DEFAULTS.dark },
-  }, {
-    setSource: () => { /* application is browser-side; value is read from the scope */ },
-    onChange: () => { /* browser half re-applies on scope publish */ },
+  ctx.inject(['settings'], (settingsCtx) => {
+    settingsCtx.settings.installSection(ctx, SKIN_CUSTOM_THEME_NAMESPACE, SkinCustomThemeConfigSchema, {
+      ...CUSTOM_THEME_DEFAULTS,
+      light: { ...CUSTOM_THEME_DEFAULTS.light },
+      dark: { ...CUSTOM_THEME_DEFAULTS.dark },
+    }, {
+      setSource: () => { /* application is browser-side; value is read from the scope */ },
+      onChange: () => { /* browser half re-applies on scope publish */ },
+    })
@@ -198,3 +202,5 @@ function applyImpl(ctx: Context): void {
-  installSettingsSection(ctx, SKIN_WALLPAPER_NAMESPACE, SkinWallpaperConfigSchema, {}, {
-    setSource: (source) => { wallpaperSource = source },
-    onChange: () => { /* routes re-read through the getter per request */ },
+  ctx.inject(['settings'], (settingsCtx) => {
+    settingsCtx.settings.installSection(ctx, SKIN_WALLPAPER_NAMESPACE, SkinWallpaperConfigSchema, {}, {
+      setSource: (source) => { wallpaperSource = source },
+      onChange: () => { /* routes re-read through the getter per request */ },
+    })
