--- a/apps/desktop-host/src/office.ts
+++ b/apps/desktop-host/src/office.ts
@@ -11,8 +11,8 @@
 export interface Config {
   /** Bundled payload directory. Missing sibling `office-skills` resources fail Host startup. */
   readonly source: string
-  /** Harness-home directory where workspace dependencies are installed. */
-  readonly root: string
+  /** Optional Harness-home directory; unset keeps dependencies in the immutable payload. */
+  readonly root?: string
 }
 
 /**
--- a/apps/desktop-host/src/index.ts
+++ b/apps/desktop-host/src/index.ts
@@ -69,10 +69,13 @@
   process.once('disconnect', () => { void stop() })
   const { ctx } = await application
   control.updateTasks = installDesktopUpdateTaskControl(ctx)
-  await ctx.plugin(desktopOffice, {
-    source: process.argv[4] ?? join(runtimeDir, '..', 'runtime', 'primary-runtime'),
-    root: join(resolveDshHome(), 'dsh-runtimes', 'dsh-primary-runtime'),
-  })
+  const configuredRuntime = process.env.DSH_PRIMARY_RUNTIME ?? process.env.DSH_BUNDLED_PRIMARY_RUNTIME
+  if (configuredRuntime !== '') {
+    await ctx.plugin(desktopOffice, {
+      source: configuredRuntime ?? process.argv[4] ?? join(runtimeDir, '..', 'runtime', 'primary-runtime'),
+      ...(configuredRuntime === undefined ? { root: join(resolveDshHome(), 'dsh-runtimes', 'dsh-primary-runtime') } : {}),
+    })
+  }
   installPlatformSessionPublisher(ctx, (session) => {
     if (process.connected) process.send?.({ type: 'platform-session', session })
   })

